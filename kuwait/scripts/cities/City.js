export const City = (city) => {
  return;

  `<div class="contentContainer__containerBottom-cityCard"
    <div class="cityCard">
        <p class="cityCard__name">${city.name}</p>
        <p class="cityCard__population">${city.population}</p>
        <p class="cityCard__description">${city.description}</p>
        <img src="${city.image}" alt="city image" class="cityCard__img"></img>
      </div>
    </div>
            `;
};
